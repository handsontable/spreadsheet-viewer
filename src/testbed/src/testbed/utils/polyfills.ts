// these polyfills are only needed for IE 11
// if the below polyfill entry points are insufficient, temporarily load the full polyfill by core-js/client/shim

import 'core-js/fn/promise';
import 'core-js/fn/object/assign';
import 'regenerator-runtime/runtime';
